# NexTerm 审计与发布评审存档

> 来源：软件研发专家委员会 Phase 0 审计 + Phase 11 发布评审 · 2026-08-26

## 一、Phase 0 项目审计结论

| 项 | 结论 |
|---|---|
| 仓库 | `~/Documents/project/NexTerm`（R-Shell 演进），git main 分支，HEAD=`FEATURE BATCH 16` |
| 状态 | HEAD 之上 25 个未提交 WIP 文件（后增至 33+），无未跟踪文件 |
| WIP 主题 | ① SSH zlib 压缩全链路移除 ② PostgreSQL 隧道 host-key TOFU 强制 pin |
| 工程体系 | pnpm / Vitest / cargo test / Playwright + WDIO 可见模式 E2E / i18n parity 检查 |
| 架构债务 | `commands.rs` 4637 行集中注册；russh `flate2` 特性为死配置（待评估移除）；本地库幽灵列 |

## 二、发布评审（Phase 11）

| 专家 | 投票 | 关键依据 |
|---|---|---|
| 产品 | PASS | 两用户故事 AC 全满足；CHANGELOG 记录 |
| UX | PASS | 对话框全屏/超时/重新信任交互；i18n 1873 键对齐 |
| 架构 | PASS | 协商统一；zlib 移除全路径闭环；无边界破坏 |
| 安全 | PASS | 凭据零泄露；服务端 fail-closed；指纹不可绕过（R1 修复后） |
| QA | PASS | vitest 687 + cargo 215 全过；Playwright 3 + WDIO 4 spec 实跑 |
| Release | READY WITH RISK | 未跑 tauri build 打包验证；TOFU e2e 零覆盖；docker 回归未入 CI |

**RELEASE DECISION：READY WITH RISK**（无 Blocking Issues）

## 三、本轮修复清单

| 编号 | 问题 | 修复 |
|---|---|---|
| P0 | `jump_integration.rs` 引用已删 `compression` 字段，集成测试编译失败 | 删除残留行 |
| Bug1 | 指纹轮换后无"重新信任"入口（安全 UX 死锁） | toast 一键重 probe + 信任对话框 |
| Bug2 | hostKey 对话框缺全屏覆盖 class | 补 `!inset-0 !m-auto` |
| Bug3 | 指纹 probe 协商算法与 SSH 不一致 | 统一 `PREFERRED_HOST_KEY_ALGOS` + 指纹实现收敛 |
| Bug4 | 指纹响应 `trusted` 字段 dead code | 删除 |
| Bug5 | connections 表幽灵 `compression` 列 | 决策：不迁移，注释说明 |
| Bug6 | `confirmHostKey` Promise 悬挂竞态 | 30s 超时 + 卸载清理 |
| R1 | PostgreSQL 隧道/probe 未禁用 zlib（安全中风险） | 两处 Config 补 `compression: [NONE]` |
| O1 | probe session 未显式断开 | 补 `disconnect` |
| 测试 | 3 个既有失败测试（transfer 契约 / proxy 适配 / folder 选择器） | 取证后修测试契约，未改业务逻辑 |

## 四、验证证据

- 前端：`vitest run` 83 文件 / **687 测试全过**；`tsc --noEmit` 0 error；eslint 0 error（3 既有 warning）
- 后端：`cargo test` **215 passed / 0 failed / 8 ignored**；`cargo check --tests` 0 error
- E2E：Playwright web-server 模式 3 passed；WDIO 桌面 smoke/sqlite/mysql/postgres-visual 逐 spec 通过
- i18n：`check-i18n-parity.mjs` 1873 keys 对齐

## 五、迭代候选（转入项目路线图）
CI 可选 job、未验证主机密钥视觉警示、TOFU e2e 场景、`host_key_verification` 收敛、warning 清理、`commands.rs` 拆分。
